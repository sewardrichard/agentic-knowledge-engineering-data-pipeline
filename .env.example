# API Configuration
WAREHOUSE_API_PORT=8000
LOGISTICS_API_BASE_URL=http://localhost:8000
FX_RATE_API_BASE_URL=http://localhost:8000

# Database Configuration
DUCKDB_PATH=./data/processed/aura.duckdb

# Pipeline Configuration
BRONZE_SCHEMA=bronze
SILVER_SCHEMA=silver
GOLD_SCHEMA=gold

# Data Sources
# Note: Use warehouse_stock.csv (generated once during setup with old timestamps)
# Do NOT regenerate on each run to preserve shadow stock scenario timestamps
WAREHOUSE_CSV_PATH=./data/raw/warehouse_stock.csv
LOGISTICS_API_ENDPOINT=/api/shipments/active
FX_RATE_ENDPOINT=/api/fx/usd-zar

# Agent Configuration
DATA_FRESHNESS_THRESHOLD_HOURS=24
RELIABILITY_THRESHOLD=0.6
SHADOW_STOCK_DETECTION_HOURS=6